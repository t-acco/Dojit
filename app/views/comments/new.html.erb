<h1>New Comment</h1>
<div class="row">
  <div class="col-md-4"">
    <p>Add new comment to <%= @post.title %></p>
    <%= render partial: 'form', locals: { topic: @topic, post: @post, comment: @comment } %>
  </div>
</div>
